const displayAnimation = {
	type: 'tween',
	duration: 0.25 // Duración de la transición en segundos
};

const justifyContentAnimation = {
	type: 'spring',
	stiffness: 400,
	damping: 30
};

export { displayAnimation, justifyContentAnimation };
